Всем привет, мы продолжаем цикл видео разбора задач по языку java, которые могут встретиться на экзамене Java SE 17
Developer. Сегодня на разборе простая задача

Какие из следующих утверджений относящиеся к фрагменту кода верны? (Выберите все правильные варианты
ответа)

Сам код и варианты ответа вы видите на экране.

Поставьте видео на паузу, напишите ваш вариант ответа в комментарии, а в конце ролика узнаете, удалось ли вам правильно
ответить на вопрос. Заодно и посмотрим, скольким по силам эта задача. Дам вам еще несколько секунд подумать

(звук таймера)

Надеюсь вы успели сделать свой выбор, потому что мы переходим к разбору этой задачи

Давайте последовательно рассмотрим варианты ответа для этой задачи

Вариант А утверждает, что строка 1 сгенерирует ошибку компиляции

В строке 1 к переменной типа short присваивается 5L. Что такое 5L описывается в разделе 3.10.1 спецификации, который
называется "Целочисленные литералы". Там написано, что целочисленный литерал имеет тип long, если к нему добавляется
буква ASCII L большая или l маленькая. в противном случае это тип int.

Таким образом в строке 1 происходит присвоение переменной типа short значения типа long

Чтобы узнать, является ли такое присвоение допустимым, обратимся к разделу 4.10.1, который описывает подтипы
для примитивных типов.
Например там написано, что float являетcя подтипом double, а long является подтипом float.
Данные отношения подтипов можно изобразить в виде направленного графа, который вы видите на экране
Например, согласно получившемуся графу тип short является подтипом типа int, который в свою очередь является подтипом
типа long

Поскольку присваивание значения родительского типа - переменной дочернего типа - недопустимо, строка 1 действительно
сгенерирует ошибку компиляции. Соответственно вариант А является верным вариантом ответа

Рассмотрим вариант Б
Вариант Б утверждает, что строка 2 сгенерирует ошибку компиляции.

В строке 2 к переменной типа int присваивается 2.0. Что такое 2.0 описывается в разделе 3.10.2 спецификации, который
называется "Литералы с плавающей точкой". Там написано, что литерал с плавающей точкой имеет тип float, если к нему
добавляется буква ASCII F большая или f маленькая. в противном случае это тип double.

Таким образом в строке 2 происходит присвоение переменной типа int значения типа double

Снова посмотрим на граф отношения подтипов примитивных типов.
Поскольку тип double является родительским по отношению к типу int присваивание, которое описано в строке 2 недопустимо,
следовательно строка 2 также сгенерирует ошибку компиляции. Таким образом вариант "Б" является верным вариантом ответа.

Теперь рассмотрим вариант C
Согласно варианту С, строка 3 сгенерирует ошибку компиляции

В строке 3 создаётся переменная типа String, к которой присваивается строковый литерал
Строковые литералы описаны в разделе 3.10.5 спецификации. Строка 3 не содержит ошибок компиляции, соответственно вариант
C НЕ является верным вариантом ответа

Рассмотрим вариант D
Согласно варианту D, строка 4 сгенерирует ошибку компиляции
В строке 4 создаётся переменная типа int, к ней присваивается выражение, согласно которому у переменной типа short
вызывается метод length
Данная строка содержит ошибку компиляции, поскольку вызов метода это недопустимая операция у примитивного типа. Список
допустимых целочисленных операций можно посмотреть в разделе 4.2.2. спецификации, который называется "Целочисленные
операции" их вы видите на экране
Таким образом Вариант D является верным вариантом ответа

Вариант E утверждает, что строка 5 сгенерирует ошибку компиляции.
В строке 5 у переменной типа double происходит обращение к свойству length. Обращение к свойству это недопустимая
операция у примитивного типа. Список допустимых операций для примитивных типов с плавающей точкой можно посмотреть в
разделе 4.2.4. спецификации.
Получается вариант E также является верным вариантом ответа

Вариант F утверждает что строка 6 сгенерирует ошибку компиляции
В строке 6 создаётся переменная типа int и ей присваивается значение, полученное вызовом метода length у переменной типа
String. Чтобы ответить на вопрос, сгенерирует ли строка 6 ошибку компиляции, обратимся к javadoc и убедимся, что у
класса String есть метод length без параметров, который возвращает int,
Таким образом вариант F является неверным вариантом ответа

Соответственно правильными ответами на задачу являются варианты A, B, D и E
Поздравляю если вы выбрали именно их

Ставьте палец вверх, если вам понравилось это видео
Пишите комментарии, если узнали что - то новое
И подпишитесь на канал, если вам интересны подобные задачи

Удачи вам на экзамене
